var test = require('tape');
var logic = require('../logic');

test('Example test', function(t) {
  t.equal(6, 6, "can compare");
  t.pass();
  t.end();
});

    // "test": "nodemon ./tests/script.test.js | tap-spec"
